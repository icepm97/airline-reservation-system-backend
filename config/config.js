const config = {
    mysql: {
        host: 'localhost',
        port: '3306',
        user: 'root',
        password: '',
        database: 'airline-reservation-system'
    },
    jwt: {
        secret: 'secret'
    }
}

module.exports = config
